import { print, setInterval, image, promptNumber, promptString, promptBoolean, csvToArray, csvToList, clear, random } from "./console";
export { print, setInterval, image, promptNumber, promptString, promptBoolean, csvToArray, csvToList, clear, random };
