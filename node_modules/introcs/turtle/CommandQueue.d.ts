import { Command } from "./Command";
export declare class CommandQueue {
    private head;
    private tail;
    enqueue(command: Command): void;
    hasNext(): boolean;
    next(): Command;
}
