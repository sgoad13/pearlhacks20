import { Command } from "./Command";
import { Context } from "../Context";
export declare class BackwardCommand extends Command {
    amount: number;
    constructor(amount: number);
    execute(context: Context, amount: number): number;
}
