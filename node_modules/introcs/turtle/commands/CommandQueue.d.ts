import { Command } from "./Command";
export declare class CommandQueue {
    private head;
    private tail;
    current(): Command;
    enqueue(command: Command): void;
    hasNext(): boolean;
    isEmpty(): boolean;
    next(): Command;
}
