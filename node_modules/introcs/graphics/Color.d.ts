export default class Color {
    static BLACK: Color;
    static WHITE: Color;
    private red;
    private green;
    private blue;
    constructor(red?: number, green?: number, blue?: number);
    toRGB(): string;
}
